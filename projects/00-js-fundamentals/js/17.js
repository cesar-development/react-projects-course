// Condicionales

const available = 4000
const retire = 200

if (available > retire) {
  // Se cumpla la condición y se ejecuta el código
  console.log('Si puedes retire')
} else {
  // no se cumple la condición, ejecutar este código
  console.log('Lo siento, no puedes retire')
}

/**
 *  > - Mayor Que
 *  < - Menor que
 *  >=  Mayor o igual
 *  <= Menor o igual
 */
